Avatar — a photo if one exists, else an initial on a deterministic color drawn from the name (so the same student always gets the same color). No default silhouette icon; the colored initial reads better at list density.

```jsx
<Avatar name="আরিফ রহমান" size={40} />
```
