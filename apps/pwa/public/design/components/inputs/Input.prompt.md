Input — a labeled text field, 48px tall (accessibility tap floor). Always pair with a visible label; `error` shows a red border + helper line, never color alone.

```jsx
<Input label="ফোন নম্বর" placeholder="01xxxxxxxxx" />
```
