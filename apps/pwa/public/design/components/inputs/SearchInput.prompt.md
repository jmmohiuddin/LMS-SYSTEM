SearchInput — a filled (not outlined) search field, used above lists (students, notices) instead of a full filter panel by default. Real filters live behind a "ফিল্টার" button (brief §10), not shown inline.

```jsx
<SearchInput placeholder="শিক্ষার্থী খুঁজুন..." />
```
