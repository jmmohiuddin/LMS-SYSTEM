Select — native `<select>` styled to match Input, same 48px height. Used for class/section/exam pickers throughout (attendance, marks entry).

```jsx
<Select label="সেকশন" options={[{value:'a',label:'এ'},{value:'b',label:'বি'}]} />
```
