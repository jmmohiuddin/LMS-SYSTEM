Sidebar — desktop/tablet primary navigation (brief §21). Full item list per role — Principal/Admin get the long list (Dashboard, Attendance, Academic, Students, Results, Finance, Reports, Settings); Teacher and Guardian get their own shorter lists, never the same sidebar for every role.

```jsx
<Sidebar active="dashboard" items={[{value:'dashboard',label:'ড্যাশবোর্ড',icon:<HomeIcon/>}]} />
```
