Table — for marks entry and result sheets on tablet/desktop. On mobile, convert rows to stacked cards instead of enabling horizontal scroll (brief §22) — Table is the desktop/tablet form, not the only form.

```jsx
<Table columns={[{key:'name',label:'নাম'},{key:'marks',label:'নম্বর',align:'right'}]} rows={[{name:'আরিফ',marks:82}]} />
```
