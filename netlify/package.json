{
  "name": "@shikhon/netlify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Netlify Functions v2 adapter over the shared Node-http service handlers.",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'"
  },
  "engines": {
    "node": ">=22"
  }
}
